# Start with dynamic DB
`npm start -- --uri localhost --port 4200 --apiKey %STEAM_API_KEY% --mode dynamic`

# Start with static DB
`npm start -- --uri localhost --port 4200 --apiKey %STEAM_API_KEY% --mode static`